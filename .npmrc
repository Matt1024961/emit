# madge's peer range doesn't list TypeScript 6 yet, so a plain `npm install`
# would fail on the peer conflict. legacy-peer-deps lets the install resolve
# the same way the lockfile was built. Remove this once madge supports TS 6.
legacy-peer-deps=true
